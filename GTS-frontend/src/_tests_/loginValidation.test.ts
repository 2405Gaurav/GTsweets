// import { validateLogin } from '../loginValidation'

// describe('validateLogin', () => {
//   it('fails when email is empty', () => {
//     expect(validateLogin('', '123456')).toEqual('Email is required')
//   })

//   it('fails when password is empty', () => {
//     expect(validateLogin('a@b.com', '')).toEqual('Password is required')
//   })

//   it('passes when email and password are valid', () => {
//     expect(validateLogin('a@b.com', '123456')).toBeNull()
//   })
// })
